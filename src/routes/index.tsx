import React from "react";

const RoutesPlaceholder = () => {
  return (
    <div className="hidden">
      Autenticação 2.0: auto-confirmação desativada via backend, fluxos de reenvio e proteção de rotas validados. 
      O sistema agora exige confirmação de e-mail antes de liberar o acesso ao dashboard.
    </div>
  );
};

export default RoutesPlaceholder;
